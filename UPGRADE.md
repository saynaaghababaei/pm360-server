# راهنمای اعمال این تغییر (حساب‌های جداگانه + سطح دسترسی)

این یه ارتقاست، نه یه دیپلوی از صفر — پروژه‌ی Railway‌ای که از قبل داری رو به‌روزرسانی می‌کنیم.

## چیزی که عوض شد

- به‌جای «یه رمز مشترک»، حالا هرکس **یوزرنیم + رمز جداگانه‌ی خودش** رو داره.
- نقش **«ادمین»**: همه‌چیز رو می‌بینه، ارزیابی جدید می‌سازه، کاربر جدید تعریف می‌کنه.
- نقش **«مشاهده‌گر»**: فقط گزارشِ **همون یک ارزیابی خاص**ی که ادمین بهش دسترسی داده رو می‌بینه — نه بیشتر، نه ساخت ارزیابی جدید.

## قدم ۱ — جایگزینی فایل‌ها

کل پوشه‌ی پروژه (`pm360-server`) روی مکت رو با نسخه‌ی جدیدی که تحویل دادم جایگزین کن (یا فایل‌به‌فایل، طبق لیستی که در ادامه‌ی چت اومده).

## قدم ۲ — ارسال به گیت‌هاب

تو Terminal، داخل پوشه‌ی پروژه:

```bash
git add .
git commit -m "افزودن حساب کاربری و سطح دسترسی"
git push
```

Railway خودش، چون به گیت‌هاب وصله، این تغییر رو می‌بینه و خودکار یه دیپلوی جدید شروع می‌کنه (چند دقیقه طول می‌کشه — می‌تونی از تب «Deployments» تو Railway پیشرفتش رو ببینی).

## قدم ۳ — به‌روزرسانی جدول‌های دیتابیس

چون یه جدول جدید («کاربر») اضافه شده، باید یه‌بار دیگه دستور migrate رو بزنی — دقیقاً مثل دفعه‌ی قبل:

۱. تو Railway، برو تو باکس `pm360-server`
۲. تب **«Console»**
۳. بزن:
```
npx prisma migrate deploy
```

## قدم ۴ — ساخت اولین حساب ادمین (خودت)

چون سیستم قبلاً هیچ کاربری نداره، باید یه‌بار از همون Console این دستور رو بزنی (به‌جای `sayna` و `یه-رمز-قوی`، یوزرنیم و رمزی که خودت می‌خوای بذار):

```
node scripts/create-user.js sayna یه-رمز-قوی admin
```

باید پیام موفقیت ببینی: `Created user "sayna" with role "admin"...`

## قدم ۵ — ورود و ساخت بقیه‌ی حساب‌ها

۱. برو همون لینک قبلی (`https://pm360-server-production.up.railway.app`)
۲. با یوزرنیم/رمزی که تو قدم ۴ ساختی وارد شو
۳. تو صفحه‌ی اصلی، رو **«مدیریت کاربران ←»** بزن
۴. از همون‌جا حساب فایزه و فرید رو با نقش **«ادمین»** بساز
۵. برای هر پروداکت منجر دیگه، یه حساب با نقش **«مشاهده‌گر»** بساز و مشخص کن دقیقاً کدوم ارزیابی رو ببینه

## نکته‌ی مهم

رمز مشترک قدیمی (`2290` یا هرچی که بود) از این به بعد **دیگه کار نمی‌کنه** — همه باید با یوزرنیم/رمز جدید وارد بشن.
