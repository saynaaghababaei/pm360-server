# راهنمای گام‌به‌گام دیپلوی (از صفر)

این راهنما فرض می‌کند هیچ حساب یا دیتابیسی از قبل ندارید. حدود ۱۰ تا ۱۵ دقیقه طول می‌کشد. همه‌چیز رایگان است (پلن‌های Free کافی‌اند برای استفاده‌ی یک تیم داخلی).

---

## قدم ۱ — ساخت دیتابیس Postgres (روش پیشنهادی: Neon)

۱. برو به [neon.tech](https://neon.tech) و با ایمیل یا گیت‌هاب ثبت‌نام کن (رایگان).
۲. یک پروژه‌ی جدید بساز (اسمش مهم نیست، مثلاً `pm360`).
۳. بعد از ساخته‌شدن، تو صفحه‌ی داشبورد پروژه، دنبال بخش **Connection String** بگرد. یه رشته شبیه این می‌بینی:
   ```
   postgresql://user:password@ep-xxxx.neon.tech/dbname?sslmode=require
   ```
۴. این رشته رو کپی کن و یه‌جا نگهش دار (تو قدم ۴ بهش نیاز داری).

*(اگه تیم فنی دیجی‌پی از قبل یه Postgres دارن، همون Connection String رو ازشون بگیر و برو قدم ۲.)*

---

## قدم ۲ — بالابردن کد رو گیت‌هاب

اگه این پوشه (پروژه) هنوز رو گیت‌هاب نیست:

۱. برو به [github.com/new](https://github.com/new) و یه ریپازیتوری جدید بساز (می‌تونه Private باشه).
۲. تو ترمینال، داخل همین پوشه‌ی پروژه:
   ```bash
   git init
   git add .
   git commit -m "اولین نسخه"
   git branch -M main
   git remote add origin <لینک ریپازیتوری خودت>
   git push -u origin main
   ```

---

## قدم ۳ — ساخت حساب Vercel و اتصال ریپازیتوری

۱. برو به [vercel.com](https://vercel.com) و با گیت‌هاب وارد شو (رایگان).
۲. رو دکمه‌ی **Add New → Project** بزن.
۳. ریپازیتوری‌ای که تو قدم ۲ ساختی رو انتخاب و **Import** کن.
۴. Vercel خودش تشخیص می‌ده این یه پروژه‌ی Next.js‌ است — نیازی به تغییر تنظیمات ساخت (Build) نیست.
۵. هنوز رو دکمه‌ی **Deploy** نزن — اول باید متغیرهای محیطی (قدم ۴) رو تنظیم کنی.

---

## قدم ۴ — تنظیم متغیرهای محیطی

تو همون صفحه‌ی تنظیمات پروژه (قبل از Deploy، یا بعداً از مسیر Project → Settings → Environment Variables)، این سه‌تا رو اضافه کن:

| نام | مقدار |
|---|---|
| `DATABASE_URL` | همون Connection String که از Neon کپی کردی (قدم ۱) |
| `APP_PASSWORD` | رمزی که می‌خوای کل تیم پروداکت باهاش وارد بشه — هرچی خواستی |
| `SESSION_SECRET` | یه رشته‌ی تصادفی و طولانی. رو ترمینال خودت بزن: `openssl rand -hex 32` و خروجیش رو کپی کن |

بعد رو **Deploy** بزن.

---

## قدم ۵ — ساخت جدول‌های دیتابیس (فقط یک‌بار)

بعد از اولین دیپلوی موفق، باید جدول‌های دیتابیس رو بسازی. رو سیستم خودت (نه رو Vercel):

۱. تو پوشه‌ی پروژه، یه فایل به اسم `.env` بساز (کنار `.env.example`) و همون سه مقدار بالا رو توش بذار — فقط `DATABASE_URL` واقعاً لازمه برای این قدم:
   ```
   DATABASE_URL="همون رشته‌ی Neon"
   ```
۲. این دستورها رو بزن:
   ```bash
   npm install
   npx prisma migrate deploy
   ```
   اگه پیام موفقیت دید (چیزی شبیه `All migrations have been successfully applied`)، جدول‌ها ساخته شدن.

**نکته:** این قدم رو فقط یک‌بار لازمه انجام بدی. هر بار که کد رو تغییر بدی و push کنی، Vercel خودش دوباره دیپلوی می‌کنه، ولی جدول‌های دیتابیس دست‌نخورده می‌مونن (مگر اینکه خودت schema رو عوض کنی و دوباره `migrate deploy` بزنی).

---

## قدم ۶ — تست نهایی

۱. لینکی که Vercel بهت داده (چیزی شبیه `https://pm360-server.vercel.app`) رو باز کن.
۲. باید صفحه‌ی لاگین رو ببینی — رمزی که تو قدم ۴ گذاشتی رو بزن.
۳. یه ارزیابی تست بساز، یه ارزیاب (مثلاً «همکار») اضافه کن، لینکش رو کپی کن و تو یه تب Incognito باز کن — باید بدون نیاز به رمز، مستقیم بره سراغ پرسشنامه.

اگه همه‌ی این‌ها کار کرد، آماده‌ی استفاده‌ی تیمه. لینک رو برای هرکسی از تیم پروداکت که می‌خوای بفرست — همه با همون یه رمز مشترک وارد می‌شن.

---

## نکات مهم برای بعداً

- **لینک بین اعضای تیم مشترکه، لاگین‌شون هم مشترکه** — یعنی هرکسی که رمز رو داره، به همه‌ی ارزیابی‌ها دسترسی داره (این دقیقاً همون چیزی بود که خواسته بودی: رمز مشترک، نه حساب جدا). اگه بعداً خواستی حساب‌های جدا با سطح دسترسی متفاوت داشته باشی، باید این بخش رو بازطراحی کنیم.
- **لینک پرسشنامه‌ی ارزیاب‌ها (`/respond/...`) عمومی و بدون رمزه** — عمداً، چون ارزیاب‌هایی مثل همکار/زیرمجموعه رمز مشترک رو ندارن. فقط کسی که لینک دقیق (توکن تصادفی و طولانی) رو داشته باشه می‌تونه بهش دسترسی داشته باشه.
- **بک‌آپ دیتابیس:** Neon به‌صورت پیش‌فرض از دیتابیست بک‌آپ می‌گیره (رجوع کن به مستندات Neon برای جزئیات پلن رایگان).
- **تغییر رمز مشترک:** کافیه مقدار `APP_PASSWORD` رو تو تنظیمات Vercel عوض کنی و یه‌بار Redeploy بزنی.

---

## اگه به مشکل خوردی

- **خطای دیتابیس هنگام استفاده:** احتمالاً قدم ۵ (`prisma migrate deploy`) درست انجام نشده یا `DATABASE_URL` تو Vercel اشتباهه.
- **صفحه‌ی لاگین لوپ می‌زنه:** `SESSION_SECRET` رو چک کن که تو Vercel درست تنظیم شده باشه.
- **بیلد رو Vercel fail می‌شه:** لاگ بیلد رو تو داشبورد Vercel ببین — معمولاً پیام خطا مشخصه چیه.
